# Backend
PORT=5000
JWT_SECRET=change_me_to_a_long_random_string
DB_FILE=./data/app.sqlite

# OTP (dev-friendly)
OTP_TTL_SECONDS=300
# When true, /api/auth/send-otp includes the OTP code in the response for testing.
OTP_DEV_RETURN_CODE=true

# Optional
NODE_ENV=development
